{%- if 'ldap' in salt['pillar.get']('postfix', '') %}

{%- set excludes = ['virtual_alias_domains'] -%}
{%- set config = salt['pillar.get']('postfix:ldap' | difference(excludes), {}) -%}
{%- for key,value in config.items() %}
{{ key }} = {{ value }}
{%- endfor %}

{%- endif %}
